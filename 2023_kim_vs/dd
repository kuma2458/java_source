echo "# vs_code_html" >> README.md
  git init
  git add README.md
  git commit -m "first commit"
  git branch -M main
  git remote add origin https://github.com/kuma2458/vs_code_html.git
  git push -u origin main
